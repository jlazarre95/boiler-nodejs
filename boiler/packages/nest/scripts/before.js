// function pluralize(name) {
//     const lastLetter = name[name.length - 1];
//     if(["s", "x"].indexOf(lastLetter)) {
//         return name + "es";
//     } else if(lastLetter === "y") {
//         return name.slice(0, name.length - 1) + "ies";
//     } else {
//         return name + "s";
//     }
// }

// boiler.params.cname = boiler.string.toCamelCase(boiler.params.name);
// boiler.params.pname = boiler.string.toPascalCase(boiler.params.name);
// boiler.params.pluralName = boiler.string.toPlural(boiler.params.name);
// boiler.params.pluralPascalCaseName = boiler.string.toPlural(boiler.params.pname);
